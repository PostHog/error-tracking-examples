name: flutter_web_releaseless
description: Flutter web app whose exceptions carry the release id posthog-cli injected into the chunk.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  posthog_flutter: ^5.38.0

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
