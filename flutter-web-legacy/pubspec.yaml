name: flutter_web_legacy
description: Flutter web app whose exceptions resolve their release through the symbol set the upload bound to it.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  posthog_flutter: ^5.38.0

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
